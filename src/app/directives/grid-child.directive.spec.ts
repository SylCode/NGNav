import { GridChildDirective } from './grid-child.directive';

describe('GridChildDirective', () => {
  it('should create an instance', () => {
    const directive = new GridChildDirective();
    expect(directive).toBeTruthy();
  });
});
